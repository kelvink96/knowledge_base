extends layout

block content
    a(href="/") back
    h1= article.title
    h6
        i Written by #{article.author}
    p= article.body
    hr
    a.btn.btn-outline-dark.mr-2(href="/articles/edit/"+article._id) Edit
    a.btn.btn-danger#deleteArticle(href="#", data-id= article._id) Delete